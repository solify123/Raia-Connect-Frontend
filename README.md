# Raia-Connect-Frontend

React Native (Expo) app for Raia-Connect — product list, filter, and checkout.

## Tech Stack

- React Native, Expo, TypeScript

## Setup

```bash
npm install
cp .env.example .env
# Set EXPO_PUBLIC_API_URL to your backend URL, then:
npx expo start
```

## Features

- Product list (name, price, category)
- Real-time filter (search / category)
- Checkout flow
